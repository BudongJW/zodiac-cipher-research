# H005：Z32 使用了 Z340 / Z408 密钥的简单变换（字母移位或反序）

- 提出日期 / 提出人：2026-09-27 / 本项目
- 目标密文：Z32
- 动机：H001 以 92% 的功效排除了“直接沿用 Z340 / Z408 密钥”。一种残余可能是 Zodiac 沿用了同一套符号分配，但对字母做了简单变换。本检验把这类变换一并排除或发现。
- 密码体制假设：与 Z340（或 Z408）共有的符号所对应的字母，经下列变换之一后成为 Z32 的明文字母：字母表移位 +1…+25（凯撒式），或字母表反序（A↔Z，即 Atbash）。其余符号字母未知（Z340：2 个；Z408：4 个）。读取顺序同 H001 的 42 种。
- 参数空间与自由度：26 种变换（25 种移位 + 反序）× 42 种读取顺序 × 自由符号（穷举或坐标上升，同 H001）。
- 打分函数与判定阈值（运行前写定）：统计量 = 所有变换与读取顺序中目标函数（AZdecrypt 5-gram × 熵项）的最高分。**p < 0.01 判为“支持”。**
- 零假设对照设计：把已知密钥的字母在其符号间随机打乱（Z340 100 次；Z408 因自由符号多、计算量大，30 次），对打乱后的密钥施加同样的 26 种变换与 42 种读取顺序，取最高分；p = (零假设最高分 ≥ 观测值的次数 + 1) / (次数 + 1)。Z408 分支的 p 值下限因此为 1/31 ≈ 0.032，**该分支只能判为“不支持”或“需扩大零假设样本后再判定”**。
- 运行记录：`python scripts/h005_key_transforms.py --model models/5-grams_english_beijinghouse_10TB_v7.gz`（种子 0）
- 结果：（运行后补充，以上各项不作修改）
- 结论：（运行后补充）
